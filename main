#include <iostream>
#include <string.h>
#include "P0000.h"


using namespace std;

int main(int argc, char** argv) {
	P0000 p[5]; // p0000객체 5개생성
	string user_name;// 이름
	string user_id;  //id
	int user_age;    //나이 

	for (int i = 0; i < 5; i++)
	{
		cout  << "번쨰 관람객 이름과 회원id,나이를 입력해주세요:";
		cin >> user_name >> user_id >> user_age; // 나이,이름,id이름받기 
		p[i].setter(user_name, user_id, user_age); //객체에 데이터 넣기
	}
	return 0;
}
